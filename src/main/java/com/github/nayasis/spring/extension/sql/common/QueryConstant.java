package com.github.nayasis.spring.extension.sql.common;

public class QueryConstant {

    public static final String PARAMETER_SINGLE = String.format( "%s/%s", QueryConstant.class.getName(), "single" );
    public static final String FOR_EACH_INDEX   = String.format( "%s/%s", QueryConstant.class.getName(), "foreach" );

}